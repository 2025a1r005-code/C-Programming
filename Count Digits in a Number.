#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    scanf("%d",&N);
    int count=0;
    if(N==0){
        printf("1");
        return 0;
    }
    while(N!=0){
    N=N/10;
    count++;
    }
    printf("%d",count);
    return 0;
}
