#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    scanf("%d",&N);
    int fact=1;
    for(int i=1;i<=N;i++){
    fact=fact*i;
    }
    printf("%d",fact);
    return 0;
}
