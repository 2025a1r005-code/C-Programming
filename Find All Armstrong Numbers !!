#include <stdio.h>
#include <math.h>
#include <stdbool.h>

bool isArmstrong(int n) {
    int original = n;
    int digits = 0;
    int sum = 0;

    int temp = n;
    while (temp > 0) {
        digits++;
        temp /= 10;
    }

    temp = n;
    while (temp > 0) {
        int digit = temp % 10;
        sum += pow(digit, digits);
        temp /= 10;
    }

    return sum == original;
}

int main() {
    int start, end;
    int found = 0;

    scanf("%d %d", &start, &end);

    for (int i = start; i <= end; i++) {
        if (isArmstrong(i)) {
            if (found) printf(" "); 
            printf("%d", i);
            found = 1;
        }
    }

    if (!found)
        printf("No Armstrong numbers found");

    return 0;
}
