#include <stdio.h>
#include <math.h>
#include <stdbool.h>

bool isPrime(int n) {
    if (n < 2)
        return false;
    for (int i = 2; i <= sqrt(n); i++) {
        if (n % i == 0)
            return false;
    }
    return true;
}

int main() {
    int start, end;
    int found = 0; 

    scanf("%d %d", &start, &end);

    for (int i = start; i <= end; i++) {
        if (isPrime(i)) {
            if (found) printf(" "); 
            printf("%d", i);
            found = 1;
        }
    }

    if ( !found)
        printf("No prime numbers found");

    return 0;
}
