#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    int arr[N];
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    int found_unique = 0; 

    for (int i = 0; i < N; i++) {
        int count = 0;
        
        for (int j = 0; j < N; j++) {
            if (arr[i] == arr[j])
                count++;
        }


        if (count == 1) {
            if (found_unique)
                printf(" ");
            printf("%d", arr[i]);
            found_unique = 1;
        }
    }

   
    if (!found_unique)
        printf("No unique elements");

    printf("\n");
    return 0;
}
