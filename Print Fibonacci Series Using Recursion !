#include <stdio.h>
int fibonacci(int n) {
    if (n == 0) return 0;
    if (n == 1) return 1;
    return fibonacci(n - 1) + fibonacci(n - 2);
}
void printFibonacci(int current, int n) {
    if (current == n)
        return;
    printf("%d ", fibonacci(current));
    printFibonacci(current + 1, n);   // recursive call to print next term
}
int main() {
    int n;
    scanf("%d", &n);
    printFibonacci(0, n);
    return 0;
}
