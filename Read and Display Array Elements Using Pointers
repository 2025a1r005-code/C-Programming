#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
    int N;
    scanf("%d",&N);
    int arr[N];
    int *x=arr;
    for(int i=0;i<N;i++){
        scanf("%d",x+i);
    }
    for(int i=0;i<N;i++){
        printf("%d ",*(x+i));
    }
    return 0;
}
