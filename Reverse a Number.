#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    int rev=0;
    scanf("%d",&N);
    while(N!=0){
        rev=rev*10;
        rev=rev+(N%10);
        N=N/10;
    }
    printf("%d",rev);
    return 0;
}
