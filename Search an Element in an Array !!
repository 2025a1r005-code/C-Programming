#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N,flag=0;
    scanf("%d",&N);
    int arr[N];
    for(int i=0;i<N;i++){
        scanf("%d",&arr[i]);
    }
    int key;
    scanf("%d",&key);
    for(int i=0;i<N;i++){
        if(key==arr[i]){
            printf("Element found at position %d",i+1);
            flag=1;
            break;
        }
    }
    if(flag==0){
        printf("Element not found");
    }

    return 0;
}
