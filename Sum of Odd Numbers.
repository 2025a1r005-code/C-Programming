#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N,sum;
    scanf("%d",&N);
    sum=0;
    for(int i=1;i<=N;i=i+2){
        sum=sum+i;
    }
   printf("%d",sum);
    return 0;
}
